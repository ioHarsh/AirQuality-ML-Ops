# Air Quality Monitoring & Prediction (Local, YAML-driven)


This project simulates sensor and weather data, runs ETL and model training, and generates daily predictions for next-day PM2.5 levels — all locally and controlled by a YAML pipeline.


## Quickstart


1. Clone or copy repository files.
2. Create and activate a virtual environment:


- Windows:
```bash
python -m venv .venv
.venv\Scripts\activate